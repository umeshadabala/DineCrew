{
  "name": "restotip-platform",
  "version": "1.0.0",
  "private": true,
  "description": "RestoTip by ParkoSpace - QR restaurant feedback and UPI tipping platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:customer\" \"npm run dev:restaurant\"",
    "dev:customer": "npm --workspace client/customer run dev",
    "dev:restaurant": "npm --workspace client/restaurant run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client/customer run build && npm --workspace client/restaurant run build",
    "start": "npm --workspace server start",
    "seed": "npm --workspace server run seed"
  },
  "workspaces": [
    "client/customer",
    "client/restaurant",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
